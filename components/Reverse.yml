component: msgflo-components/Reverse
label: Reverse the input.
icon: file-word-o
inports:
  in:
    description: The input
    type: string
    queue: '#ROLE.IN'
outports:
  out:
    description: The input reversed.
    type: string
    queue: '#ROLE.OUT'
