component: msgflo-components/Append
label: Append a string to another string.
icon: file-alt-o
inports:
  const:
    description: The string that will be appended to
    type: string
    queue: '#ROLE.CONST'
  in:
    description: The string to be appended
    type: string
    queue: '#ROLE.IN'
outports:
  out:
    description: The output.
    type: string
    queue: '#ROLE.OUT'
