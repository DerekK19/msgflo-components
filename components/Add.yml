component: msgflo-components/Add
label: Add the inputs.
icon: file-cog-o
inports:
  const:
    description: The constant that will be added to
    type: number
    queue: '#ROLE.CONST'
  in:
    description: The input
    type: number
    queue: '#ROLE.IN'
outports:
  out:
    description: The output.
    type: number
    queue: '#ROLE.OUT'
